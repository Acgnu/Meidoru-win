<local:BasePage x:Class="AcgnuX.Pages.MobileDeviceControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AcgnuX.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MobileDeviceControl">

    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <!--主内容Frame-->
            <Frame Name="ContentFrame" Grid.Column="0" Grid.Row="0" Source="SyncPictures.xaml" NavigationUIVisibility="Hidden" />
        </Grid>
        <!--刷新按钮-->
        <Button Style="{StaticResource RefreshFloatButtonStyle}" Click="OnSwitchButtonClick">
            <Path Style="{StaticResource RefreshPathIcoStyle}"/>
        </Button>
    </Grid>
</local:BasePage>
